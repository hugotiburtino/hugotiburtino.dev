import { ThirdPartyProject } from './third-party-project';

describe('ThirdPartyProject', () => {
  it('should create an instance', () => {
    expect(new ThirdPartyProject()).toBeTruthy();
  });
});
